[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
  ".venv",
  "demo_web",
]

[tool.ruff.lint]
select = [
  "E", "F", "I",
]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]

