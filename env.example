# Document AI Framework Configuration
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED: OpenAI Configuration
# ===========================================
# Your OpenAI API key (required)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: OpenAI API base URL (defaults to https://api.openai.com/v1)
# OPENAI_API_BASE_URL=https://api.openai.com/v1

# Optional: Model name (defaults to gpt-5)
# Supported: gpt-5, gpt-4o, gpt-4o-mini, gpt-4, gpt-4-turbo, gpt-3.5-turbo
MODEL_NAME=gpt-5

# ===========================================
# OCR Configuration
# ===========================================
# Optional: Path to tesseract binary (leave empty if tesseract is in PATH)
# TESSERACT_CMD=/usr/bin/tesseract

# Optional: Path to poppler utils directory
# POPPLER_PATH=/usr/bin

# Optional: OCR language (defaults to 'eng')
# OCR_LANG=eng

# ===========================================
# API Security & Limits
# ===========================================
# Optional: Comma-separated list of allowed Bearer tokens
# If set, API will require authentication
# ALLOWED_TOKENS=token1,token2,token3

# Optional: Maximum file size in MB (defaults to 15)
MAX_FILE_SIZE_MB=15

# Optional: Allow processing files from URLs (defaults to true)
ALLOW_FILE_URLS=true

# Optional: Maximum concurrent processing tasks (defaults to 4)
MAX_CONCURRENCY=4

# Optional: Rate limit per client IP per minute (defaults to 60)
RATE_LIMIT_PER_MIN=60

# ===========================================
# Demo Web App Configuration
# ===========================================
# Optional: Base URL for the document API (defaults to http://127.0.0.1:8080)
DOC_API_BASE_URL=http://127.0.0.1:8080

# Optional: API token for demo app to authenticate with API
# DOC_API_TOKEN=your-api-token-here

# ===========================================
# Service Type
# ===========================================
# Optional: Which service to run (api or demo, defaults to api)
# SERVICE_TYPE=api
